@model dynamic
@{
    // if somebody actually uses these auto-generated passwords I will *find them*
    var rand = new Random();
    string[] passwordLeading = new string[] { "password", "god", "swordfish", "peaches", "baseball", "badboy", "rosebud", "guitar", "butter", "jaguar", "nascar", "wizard", "sunshine", "aaaa" };
    char[] passwordTrailing = new char[] { '!', '@', '#', '$', '%', '^', '&', '*', '(', ')', '-', '+', '=', '`', '~' };
    
    
    var word = passwordLeading[rand.Next(passwordLeading.Length)];
    var startInt = rand.Next(6) + 1;
    var trailing = passwordTrailing[rand.Next(passwordTrailing.Length)];

    var fakeNewPassword = word + startInt;
    fakeNewPassword += (startInt + 1);
    fakeNewPassword += (startInt + 2);
    fakeNewPassword += trailing;
}

Reset your password on @Model.SiteName  

###Here's your password: *@(fakeNewPassword)*  
  

Just kidding!

To reset the password on your @Model.SiteName account please follow [this link][1].

(Someone requested a password reset for the @Model.SiteName account associated with this email address; if it was not you, please ignore this email.)

  [1]: @Model.RecoveryLink